import moment from "moment";

export const minSliderValue = moment().weekday();
export const maxSliderValue = moment().weekday() + 7;

export const basicBlue = "#0970ce";
export const darkBlue = "#01407a";
export const lightBlue = "#8bc7ff";
